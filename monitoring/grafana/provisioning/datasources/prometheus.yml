# Auto-provision the Prometheus datasource so Grafana works on first boot (no manual setup).
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
