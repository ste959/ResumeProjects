# Pinned to the versions this stack is developed/tested against (reproducibility).
# A numpy/pandas major bump can silently change behaviour, so pin rather than float.
numpy==2.4.2
pandas==3.0.1
pyarrow==24.0.0
duckdb==1.5.4
requests==2.34.2
scikit-learn==1.8.0
scipy==1.17.1
pytest==9.1.1
yfinance>=0.2  # long-history daily bars (run_longtest.py)
