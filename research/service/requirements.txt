# Research service — extends the research layer's own pinned deps (../requirements.txt) with the web
# framework. Install both:  pip install -r ../requirements.txt -r requirements.txt
fastapi==0.141.1
uvicorn[standard]==0.34.0
httpx==0.28.1
prometheus-fastapi-instrumentator==7.0.0
