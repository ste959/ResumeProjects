# Copy to .env to override defaults.
# Base URL for the API in a built deployment. Leave empty in dev — the Vite proxy
# forwards /api to the backend (see vite.config.ts).
VITE_API_BASE=

# Where the dev-server proxy forwards /api (defaults to http://localhost:8080).
VITE_API_TARGET=http://localhost:8080
